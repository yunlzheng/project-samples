spring:
  application:
    name: kube-application
  cloud:
    kubernetes:
      client:
        namespace: yunlong
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        sources:
          - name: ${spring.application.name}
