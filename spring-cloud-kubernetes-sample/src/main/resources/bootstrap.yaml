spring:
  application:
    name: kube-application
    namespace: yunlong
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        sources:
          - name: ${spring.application.name}
            namespace: ${spring.application.namespace}